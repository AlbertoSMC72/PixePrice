<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permite compartir archivos desde el subdirectorio 'images' dentro del caché -->
    <cache-path
        name="shared_camera_images"
        path="images/" />
    <!-- Podrías añadir otras rutas aquí si fuera necesario, por ejemplo: -->
    <!-- <files-path name="my_files" path="." /> -->
    <!-- <external-path name="my_external_files" path="."/> -->
</paths>